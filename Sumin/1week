#2908 문제


import sys
input = sys.stdin.readline

a, b = input().split()
# 문자열 뒤집기 를 구글링 했다..
# 문자열[시작:끝] 과 같으며, 앞에서부터 슬라이싱 해서 새로운 문자열은 만드는 함수라고 한다..
# 따라서 [::2]는 인덱스 [0],[2],[4] .. 이런식으로 잘라 붙여 새로운 문자열을 만든다..
# [::-1] 은 맨뒤 인덱스부터 한칸씩 붙인다는 소리다 
a = a[::-1]
b = b[::-1]

# 둘중 큰값이 나와야 하므로 max 함수 사용 
print(max(a, b))
